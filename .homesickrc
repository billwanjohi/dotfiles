#!/usr/bin/env ruby
# three blocks follow

# osx
```
sh $HOME/.homesick/dotfiles/install_zsh.sh
cp $HOME/.config/git/user.example $HOME/.config/git/user

ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
brew update
brew install caskroom/cask/brew-cask
brew install python


# gvm install
gvm install go1.42
gvm use go1.42 --default

# rbenv install
ln -s ../../rbenv-plugins $HOME/.rbenv/plugins
rbenv install 2.2.0
rbenv global 2.2.0
rbenv rehash
gem update --system

gem install rake
pushd ~/.vim && rake popd  # janus
```

`brew doctor # pay attention to this output`

# other mac tweaks
# show scroll bars: always
# turn on filevault
